const Categoria = require('./categoria');
const Rol = require('./role');
const Usuario = require('./user');
const Producto = require('./producto');

module.exports = {
  Categoria, Rol, Usuario, Producto
}
